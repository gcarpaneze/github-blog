import { styled } from '../../styles/stitches.config'

export const Head = styled('header', {
  width: '100%',
  height: '18.5rem',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',

  img: {
    width: '100%',
  },
})
